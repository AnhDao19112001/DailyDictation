export const authenClient = (token) => {
    return {
        type: "SUCCESS",
        token: token
    }
}
export const authenAdmin = (token) => {
    return {
        type: "SUCCESS",
        token: token
    }
}